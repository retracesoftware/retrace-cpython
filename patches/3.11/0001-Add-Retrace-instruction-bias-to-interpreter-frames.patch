From 34c4fb38b6e8b57dc004d6c3a1d04ea35ed48115 Mon Sep 17 00:00:00 2001
From: nathan-retrace <nathan@retracesoftware.com>
Date: Tue, 5 May 2026 21:55:01 +0100
Subject: [PATCH 1/5] Add Retrace instruction bias to interpreter frames

---
 Include/internal/pycore_frame.h | 21 +++++++++++++++++++++
 Python/ceval.c                  | 17 +++++++++++++++--

diff --git a/Include/internal/pycore_frame.h b/Include/internal/pycore_frame.h
index 4866ea2..ac4fc92 100644
--- a/Include/internal/pycore_frame.h
+++ b/Include/internal/pycore_frame.h
@@ -8,2 +8,3 @@ extern "C" {
 #include <stddef.h>
+#include <stdint.h>
 
@@ -64,2 +65,3 @@ typedef struct _PyInterpreterFrame {
     char owner;
+    int64_t retrace_instr_bias;
     /* Locals and stack */
@@ -71,2 +73,20 @@ typedef struct _PyInterpreterFrame {
 
+static inline int64_t
+_PyFrame_RetraceInstructionCoordinate(_PyInterpreterFrame *frame)
+{
+    return frame->retrace_instr_bias + (int64_t)_PyInterpreterFrame_LASTI(frame);
+}
+
+static inline void
+_PyFrame_RetraceInstructionJump(
+    _PyInterpreterFrame *frame,
+    _Py_CODEUNIT *from,
+    _Py_CODEUNIT *to)
+{
+    PyCodeObject *code = frame->f_code;
+    int from_offset = (int)(from - _PyCode_CODE(code));
+    int to_offset = (int)(to - _PyCode_CODE(code));
+    frame->retrace_instr_bias += (int64_t)from_offset + 1 - to_offset;
+}
+
 static inline PyObject **_PyFrame_Stackbase(_PyInterpreterFrame *f) {
@@ -115,2 +135,3 @@ _PyFrame_InitializeSpecials(
     frame->owner = FRAME_OWNED_BY_THREAD;
+    frame->retrace_instr_bias = 0;
 }
diff --git a/Python/ceval.c b/Python/ceval.c
index 1ce6bbc..4ded04c 100644
--- a/Python/ceval.c
+++ b/Python/ceval.c
@@ -1366,4 +1366,14 @@ eval_frame_handle_pending(PyThreadState *tstate)
     } while (0)
-#define JUMPTO(x)       (next_instr = first_instr + (x))
-#define JUMPBY(x)       (next_instr += (x))
+#define JUMPTO(x) \
+    do { \
+        _Py_CODEUNIT *target = first_instr + (x); \
+        _PyFrame_RetraceInstructionJump(frame, frame->prev_instr, target); \
+        next_instr = target; \
+    } while (0)
+#define JUMPBY(x) \
+    do { \
+        _Py_CODEUNIT *target = next_instr + (x); \
+        _PyFrame_RetraceInstructionJump(frame, frame->prev_instr, target); \
+        next_instr = target; \
+    } while (0)
 
@@ -5644,2 +5654,3 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
                     for expository comments */
+                    _Py_CODEUNIT *retrace_jump_src = next_instr;
                     _PyFrame_SetStackPointer(frame, stack_pointer);
@@ -5668,2 +5679,4 @@ _PyEval_EvalFrameDefault(PyThreadState *tstate, _PyInterpreterFrame *frame, int
                     next_instr = frame->prev_instr;
+                    _PyFrame_RetraceInstructionJump(
+                        frame, retrace_jump_src, next_instr);
                 }
-- 
2.50.1 (Apple Git-155)

