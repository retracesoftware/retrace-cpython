From 78b9302aed761ed5d5aa2625b11f9caf2fe89037 Mon Sep 17 00:00:00 2001
From: Retrace CPython Builder <retrace-cpython@retracesoftware.com>
Date: Thu, 7 May 2026 09:50:42 +0100
Subject: [PATCH 3/7] Add retrace thread scheduling callbacks

---
 Include/cpython/pystate.h        |  4 ++++
 Include/internal/pycore_interp.h |  3 +++
 Modules/_threadmodule.c          | 19 +++++++++++++++----
 Modules/signalmodule.c           |  3 +++
 Python/ceval_gil.c               | 14 ++++++++++++--
 Python/pystate.c                 | 12 +++++++++---
 6 files changed, 52 insertions(+), 10 deletions(-)

diff --git a/Include/cpython/pystate.h b/Include/cpython/pystate.h
index 95fad89..cdb861b 100644
--- a/Include/cpython/pystate.h
+++ b/Include/cpython/pystate.h
@@ -2,6 +2,8 @@
 #  error "this header file must not be included directly"
 #endif

+#include "cpython/retrace_state.h" // _PyRetraceThreadState
+

 /*
 Runtime Feature Flags
@@ -246,6 +248,8 @@ struct _ts {

     /* The bottom-most frame on the stack. */
     _PyCFrame root_cframe;
+
+    _PyRetraceThreadState retrace;
 };

 /* WASI has limited call stack. Python's recursion limit depends on code
diff --git a/Include/internal/pycore_interp.h b/Include/internal/pycore_interp.h
index 37cc88e..2cd432e 100644
--- a/Include/internal/pycore_interp.h
+++ b/Include/internal/pycore_interp.h
@@ -33,6 +33,7 @@ extern "C" {
 #include "pycore_typeobject.h"    // struct type_cache
 #include "pycore_unicodeobject.h" // struct _Py_unicode_state
 #include "pycore_warnings.h"      // struct _warnings_runtime_state
+#include "cpython/retrace_state.h" // _PyRetraceInterpreterState


 struct _Py_long_state {
@@ -181,6 +182,8 @@ struct _is {
     struct _obmalloc_state obmalloc;

     PyObject *audit_hooks;
+
+    _PyRetraceInterpreterState retrace;
     PyType_WatchCallback type_watchers[TYPE_MAX_WATCHERS];
     PyCode_WatchCallback code_watchers[CODE_MAX_WATCHERS];
     // One bit is set for each non-NULL entry in code_watchers
diff --git a/Modules/_threadmodule.c b/Modules/_threadmodule.c
index 38107f0..e2ce6fa 100644
--- a/Modules/_threadmodule.c
+++ b/Modules/_threadmodule.c
@@ -5,4 +5,5 @@
 #include "Python.h"
+#include "pycore_retrace.h"       // retrace thread helpers
 #include "pycore_interp.h"        // _PyInterpreterState.threads.count
 #include "pycore_moduleobject.h"  // _PyModule_GetState()
 #include "pycore_pylifecycle.h"
@@ -1225,5 +1226,6 @@ thread_PyThread_start_new_thread(PyObject *self, PyObject *fargs)
         thread_bootstate_free(boot, 1);
         return NULL;
     }
+    _PyRetrace_RegisterThreadIdent(boot->tstate, ident);
-    return PyLong_FromUnsignedLong(ident);
+    return _PyRetrace_ThreadIdentObject(boot->tstate);
 }
@@ -1299,8 +1301,3 @@ thread_get_ident(PyObject *self, PyObject *Py_UNUSED(ignored))
 {
-    unsigned long ident = PyThread_get_thread_ident();
-    if (ident == PYTHREAD_INVALID_THREAD_ID) {
-        PyErr_SetString(ThreadError, "no current thread ident");
-        return NULL;
-    }
-    return PyLong_FromUnsignedLong(ident);
+    return _PyRetrace_ThreadIdentObject(_PyThreadState_GET());
 }
@@ -1485,19 +1482,21 @@ thread_excepthook_file(PyObject *file, PyObject *exc_type, PyObject *exc_value,
         Py_DECREF(name);
     }
     else {
-        unsigned long ident = PyThread_get_thread_ident();
-        PyObject *str = PyUnicode_FromFormat("%lu", ident);
+        PyObject *ident = _PyRetrace_ThreadIdentObject(_PyThreadState_GET());
+        if (ident == NULL) {
+            PyErr_Clear();
+        }
+        PyObject *str = ident == NULL ? NULL : PyObject_Str(ident);
+        Py_XDECREF(ident);
         if (str != NULL) {
             if (PyFile_WriteObject(str, file, Py_PRINT_RAW) < 0) {
                 Py_DECREF(str);
                 return -1;
             }
             Py_DECREF(str);
         }
         else {
-            PyErr_Clear();
-
             if (PyFile_WriteString("<failed to get thread name>", file) < 0) {
                 return -1;
             }
         }
     }
diff --git a/Modules/signalmodule.c b/Modules/signalmodule.c
index 60d3a54..1fbc84c 100644
--- a/Modules/signalmodule.c
+++ b/Modules/signalmodule.c
@@ -13,4 +13,5 @@
 #include "pycore_moduleobject.h"  // _PyModule_GetState()
 #include "pycore_pyerrors.h"      // _PyErr_SetString()
 #include "pycore_pystate.h"       // _PyThreadState_GET()
+#include "pycore_retrace.h"       // retrace thread helpers
 #include "pycore_signal.h"        // Py_NSIG
@@ -1282,6 +1283,8 @@ signal_pthread_kill_impl(PyObject *module, unsigned long thread_id,
         return NULL;
     }
-
+
+    thread_id = _PyRetrace_CPythonThreadIdentFromPublicIdent(
+        _PyInterpreterState_GET(), thread_id);
     err = pthread_kill((pthread_t)thread_id, signalnum);
     if (err != 0) {
         errno = err;
diff --git a/Python/ceval_gil.c b/Python/ceval_gil.c
index c1ab588..6bd4ec4 100644
--- a/Python/ceval_gil.c
+++ b/Python/ceval_gil.c
@@ -7,6 +7,7 @@
 #include "pycore_initconfig.h"    // _PyStatus_OK()
 #include "pycore_interp.h"        // _Py_RunGC()
 #include "pycore_pymem.h"         // _PyMem_IsPtrFreed()
+#include "pycore_retrace.h"       // thread scheduling callbacks

 /*
    Notes about the implementation:
@@ -411,9 +412,12 @@ take_gil(PyThreadState *tstate)
     _Py_atomic_store_relaxed(&gil->locked, 1);
     _Py_ANNOTATE_RWLOCK_ACQUIRED(&gil->locked, /*is_write=*/1);

-    if (tstate != (PyThreadState*)_Py_atomic_load_relaxed(&gil->last_holder)) {
+    PyThreadState *previous_holder =
+        (PyThreadState*)_Py_atomic_load_relaxed(&gil->last_holder);
+    if (tstate != previous_holder) {
         _Py_atomic_store_relaxed(&gil->last_holder, (uintptr_t)tstate);
         ++gil->switch_number;
+        _PyRetrace_NoteThreadResume(tstate);
     }

 #ifdef FORCE_SWITCHING
@@ -596,6 +600,7 @@ PyEval_AcquireLock(void)
     _Py_EnsureTstateNotNULL(tstate);

     take_gil(tstate);
+    _PyRetrace_DeliverThreadResumeCallback(tstate);
 }

 void
@@ -614,6 +619,7 @@ _PyEval_AcquireLock(PyThreadState *tstate)
 {
     _Py_EnsureTstateNotNULL(tstate);
     take_gil(tstate);
+    _PyRetrace_DeliverThreadResumeCallback(tstate);
 }

 void
@@ -636,6 +642,7 @@ PyEval_AcquireThread(PyThreadState *tstate)
     if (_PyThreadState_SwapNoGIL(tstate) != NULL) {
         Py_FatalError("non-NULL old thread state");
     }
+    _PyRetrace_DeliverThreadResumeCallback(tstate);
 }

 void
@@ -708,6 +715,7 @@ PyEval_RestoreThread(PyThreadState *tstate)
     take_gil(tstate);

     _PyThreadState_SwapNoGIL(tstate);
+    _PyRetrace_DeliverThreadResumeCallback(tstate);
 }


@@ -770,6 +778,7 @@ _push_pending_call(struct _pending_calls *pending,
     return 0;
 }

+
 static int
 _next_pending_call(struct _pending_calls *pending,
                    int (**func)(void *), void **arg)
@@ -1048,6 +1057,7 @@ _Py_HandlePending(PyThreadState *tstate)
     /* GIL drop request */
     if (_Py_atomic_load_relaxed_int32(&interp_ceval_state->gil_drop_request)) {
         /* Give another thread a chance */
+        _PyRetrace_DeliverThreadYieldCallback(tstate);
         if (_PyThreadState_SwapNoGIL(NULL) != tstate) {
             Py_FatalError("tstate mix-up");
         }
@@ -1060,6 +1070,7 @@ _Py_HandlePending(PyThreadState *tstate)
         if (_PyThreadState_SwapNoGIL(tstate) != NULL) {
             Py_FatalError("orphan tstate");
         }
+        _PyRetrace_DeliverThreadResumeCallback(tstate);
     }

     /* Check for asynchronous exception. */
@@ -1086,4 +1097,3 @@ _Py_HandlePending(PyThreadState *tstate)

     return 0;
 }
-
diff --git a/Python/pystate.c b/Python/pystate.c
index 6aed9ac..c8823e8 100644
--- a/Python/pystate.c
+++ b/Python/pystate.c
@@ -8,6 +8,7 @@
 #include "pycore_pyerrors.h"
 #include "pycore_pylifecycle.h"
 #include "pycore_pymem.h"         // _PyMem_SetDefaultAllocator()
+#include "pycore_retrace.h"       // thread-state helpers
 #include "pycore_pystate.h"
 #include "pycore_runtime_init.h"  // _PyRuntimeState_INIT
 #include "pycore_sysmodule.h"
@@ -851,6 +851,8 @@ interpreter_clear(PyInterpreterState *interp, PyThreadState *tstate)
     // XXX Make sure we properly deal with problematic finalizers.

     Py_CLEAR(interp->audit_hooks);
+    Py_CLEAR(interp->retrace.thread_yield_callback);
+    Py_CLEAR(interp->retrace.thread_resume_callback);

     for (int i = 0; i < _PY_MONITORING_UNGROUPED_EVENTS; i++) {
         interp->monitors.tools[i] = 0;
@@ -1363,6 +1365,7 @@ init_threadstate(PyThreadState *tstate,
     tstate->datastack_top = NULL;
     tstate->datastack_limit = NULL;
     tstate->what_event = -1;
+    _PyRetrace_InitializeThreadState(interp, tstate);

     tstate->_status.initialized = 1;
 }
@@ -1558,5 +1561,6 @@ PyThreadState_Clear(PyThreadState *tstate)
     Py_CLEAR(tstate->context);
-
+    _PyRetrace_ClearThreadState(tstate);
+
     if (tstate->on_delete != NULL) {
         tstate->on_delete(tstate->on_delete_data);
     }
@@ -1433,6 +1438,7 @@ new_threadstate(PyInterpreterState *interp)

     init_threadstate(tstate, interp, id);
     add_threadstate(interp, tstate, old_head);
+    _PyRetrace_SeedThreadState(current_fast_get(runtime), tstate);

     HEAD_UNLOCK(runtime);
     if (!used_newtstate) {
@@ -1819,6 +1825,6 @@ PyThreadState_SetAsyncExc(unsigned long id, PyObject *exc)
      */
     HEAD_LOCK(runtime);
     for (PyThreadState *tstate = interp->threads.head; tstate != NULL; tstate = tstate->next) {
-        if (tstate->thread_id != id) {
+        if (!_PyRetrace_ThreadMatchesAsyncExcId(interp, tstate, id)) {
             continue;
         }
@@ -2023,7 +2029,7 @@ _PyThread_CurrentFrames(void)
             if (frame == NULL) {
                 continue;
             }
-            PyObject *id = PyLong_FromUnsignedLong(t->thread_id);
+            PyObject *id = _PyRetrace_ThreadIdentObject(t);
             if (id == NULL) {
                 goto fail;
             }
@@ -2086,7 +2092,7 @@ _PyThread_CurrentExceptions(void)
             if (err_info == NULL) {
                 continue;
             }
-            PyObject *id = PyLong_FromUnsignedLong(t->thread_id);
+            PyObject *id = _PyRetrace_ThreadIdentObject(t);
             if (id == NULL) {
                 goto fail;
             }
--
2.50.1 (Apple Git-155)
